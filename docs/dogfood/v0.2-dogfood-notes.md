# v0.2 — 3-day dogfood notes

**Plan item:** [v0.2 plan — Task 12 "v0.2 capstone — hardening review + release"](../plan/2026-04-20-v0.2-atlassian.md)
**Branch (scaffold):** `DAY-84-v0.2.0-capstone`
**Semver label:** `semver:none` (this doc) / `semver:patch` (the v0.2.1 hotfix PR that accompanies it)
**Owner:** `_<your name>_`

> **Status (recorded 2026-04-21).** This file is **scaffolded but not yet filled in** as part of the v0.2.1 hotfix PR ([DAY-84](https://github.com/vedanthvdev/dayseam/issues/?)). The seven P0/HIGH fixes from the capstone review ([`docs/review/v0.2-review.md`](../review/v0.2-review.md)) land *before* the dogfood sweep, not after, because four of them (`DOG-v0.2-01/02/03`, `CORR-v0.2-01`) block Atlassian from working at all — dogfooding a broken build would add no signal and would risk shipping a real PAT to a non-Atlassian host on the first Validate click (DOG-v0.2-03).
>
> Once v0.2.1 merges and publishes, the owner runs the three-day sweep against the published DMG and fills §1 + §2 + §3 + §4 below. The cross-cutting analysis feeds the v0.3 plan's "lessons from v0.2 dogfood" subsection.

This is the written artifact of the v0.2 dogfood sweep. It mirrors the shape of [`phase-2-dogfood-notes.md`](./phase-2-dogfood-notes.md) — one setup snapshot, three daily EOD entries, one cross-cutting analysis — tuned to exercise the v0.2 Atlassian surface specifically (Jira transitions + comments + assignments; Confluence page creates + edits + comments; shared-PAT Journey A; separate-PAT Journey B; reuse-PAT Journey C; cross-source MR↔issue enrichment).

---

## 1. Setup snapshot

Filled in once, at the start of Day 1.

| Field                  | Value                                  |
|------------------------|----------------------------------------|
| Build SHA (Day 1)      | `<v0.2.1 tag>`                         |
| Build SHA (Day 2+3)    | `<v0.2.1 tag or subsequent HEAD>`      |
| Tauri dev or packaged  | Packaged universal unsigned DMG from the [GitHub Release](https://github.com/vedanthvdev/dayseam/releases) |
| macOS version          | `<arm64 14.x / 15.x>`                  |
| Fresh or upgraded DB   | Fresh `state.db` for Day 1; upgraded for Day 2+3 |
| GitLab scan target     | `<workspace>`                          |
| Local-git scan roots   | `<~/Code/…>`                           |
| Atlassian workspace    | `<workspace-slug>.atlassian.net`       |
| Atlassian products     | Jira + Confluence (Journey A shared PAT) |
| Atlassian email        | `<redacted>`                           |
| Identities configured  | One git email + one GitLab user id + one `AtlassianAccountId` (seeded by `atlassian_sources_add`) |
| Sinks configured       | One markdown-file sink under `~/Documents/dayseam/` |
| Self-person name       | `Me` sentinel                          |

Anything surprising about the Atlassian first-run path (workspace-URL normalisation, `.atlassian.net` rejection on typo, product-checkbox flow, shared-PAT vs separate-PAT Journey selection) goes in Day 1 §3 rather than here.

---

## 2. Day-by-day EOD entries

Three consecutive real workdays. Each entry is one paragraph on what the day covered (what code/tickets the user actually worked on), followed by a table of friction points encountered while generating and saving the report. Friction points that took less than 5 minutes to fix land as an inline patch in §2.N.1; everything else becomes a finding row in §4.

### 2.1 Day 1 — `<YYYY-MM-DD>`

**Work covered (real content):** `_<one paragraph>_`

**Dogfood session:**

| Time | Step                                          | Observation | Friction? |
|------|-----------------------------------------------|-------------|-----------|
| `HH:MM` | Launch app; **+** → Jira / Confluence        | `_…_`       | `_…_`     |
| `HH:MM` | Paste workspace URL; check normalisation    | `_…_`       | `_…_`     |
| `HH:MM` | Fill email + API token; click Validate      | `_…_`       | `_…_`     |
| `HH:MM` | Click Add source; confirm source appears in sidebar | `_…_` | `_…_`     |
| `HH:MM` | Generate report for today                   | `_…_`       | `_…_`     |
| `HH:MM` | Save to markdown sink; re-open saved file   | `_…_`       | `_…_`     |

**Per-kind sanity checks:** Jira bullets appear under `## Jira` (not `## Commits`). Confluence bullets appear under `## Confluence`. A Jira issue transitioned by someone else does **not** appear (self-filter working). A Confluence page I edited that was created by someone else appears as edited, not created. Cross-source enrichment: a GitLab MR whose title mentions `<ticket-key>` shows the `(triggered by !<iid>)` annotation on the related Jira transition.

**§2.1.1 Inline patches landed during Day 1:** _none_ / `_commit SHA + one-line intent_`

### 2.2 Day 2 — `<YYYY-MM-DD>`

_(same shape as §2.1; exercises Journey B or C on a second source pair if applicable)_

### 2.3 Day 3 — `<YYYY-MM-DD>`

_(same shape; one PAT-rotation path exercised via `sources_update` to verify `ensure_atlassian_self_identity` keeps the self-filter honest)_

---

## 3. First-run path observations

Free-form notes on anything the Add-Atlassian dialog, sidebar, or report composer did that surprised the dogfood owner on Day 1. Focus areas:

- Workspace URL normalisation (slug → canonical URL; non-`.atlassian.net` → invalid)
- Email + API token validation (401 / 403 error copy; `action_hint: "reconnect"`)
- Journey selection (shared PAT A vs separate PAT B vs reuse-existing C)
- "Restart required" toast — still needed after DOG-v0.2-02 fix? If yes, document *why* (keychain re-read? something else?)
- Report composer chip ordering; sidebar health-dot semantics
- Per-source error cards (exercise by revoking the API token mid-run)

---

## 4. Findings from the sweep

Anything that took more than 5 minutes to work around, or that would fail a future automated test, lands here as a finding row. Each finding gets a one-sentence issue filed on GitHub and either an inline fix in the v0.2.2 PR (the companion to this doc) or a deferred-to-v0.3 disposition.

| ID | Day | Severity | Title | Disposition |
|----|-----|----------|-------|-------------|
| DOGFOOD-v0.2-01 | `_…_` | `_…_` | `_…_` | `_Inlined / Deferred → #…_` |

---

## 5. Cross-cutting analysis

One or two paragraphs on patterns that showed up across multiple days — e.g. "every report with both Jira and Confluence activity makes me want a `## Atlassian` super-section" or "the `(triggered by !<iid>)` annotation fires too aggressively when MR and transition are hours apart". Feeds the v0.3 plan.

---

## 6. Exit criteria

- [ ] Three real workdays of Atlassian-bearing reports saved to the markdown sink
- [ ] At least one Jira bullet, one Confluence bullet, and one cross-source-enriched transition exercised end-to-end against the published v0.2.1 DMG
- [ ] All §4 findings either inlined (in a companion `semver:patch` PR) or filed as a tracked issue with a linked disposition
- [ ] §3 first-run observations summarised into one paragraph for the v0.3 plan
